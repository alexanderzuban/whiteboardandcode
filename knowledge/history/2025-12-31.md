# Prompts - 2025-12-31

## Session 1

### Prompt 1
> Create or update my CLAUDE.MD file

**Outcome:** Created `/CLAUDE.md` with comprehensive project documentation including:
- Project overview
- Tech stack details
- Project structure
- Commands
- Architecture patterns
- Key files reference
- Development notes

---

### Prompt 2
> under project create a folder with the name "knowledge" with the following subfolders "documentation", "features", "history". "knowledge->documentation" - create documentation files with the requirements, "knowledge-features" - create documention/architecture/api for implemented features, "knowledge-history" - keep all the promths to claude in text files. Collect all the prompth in the day files. Add to .CLAUDE instructions to revies/maintain all this after each change.

**Outcome:** Created knowledge management structure:

1. **Created folder structure:**
   - `/knowledge/documentation/` - Requirements and specs
   - `/knowledge/features/` - Feature documentation
   - `/knowledge/history/` - Prompt history (this file)

2. **Documentation files created:**
   - `documentation/README.md`
   - `documentation/requirements.md`

3. **Feature documentation created:**
   - `features/README.md`
   - `features/drawing-system.md`
   - `features/shapes.md`
   - `features/operations.md`
   - `features/state-management.md`
   - `features/text-editor.md`

4. **History files created:**
   - `history/README.md`
   - `history/2025-12-31.md` (this file)

5. **Updated CLAUDE.md** with knowledge base maintenance instructions

---

### Prompt 3
> The project goal is to provide an easy collaboration environment with ability to write code/use whiteboard and modeling tools. Right now it is hosted on AWS serverless platform. reorganize it in a way that backend can be added to it. suggest which backend framework will be the best/cheapest option, so based on this reorganize this repo structure to include/define backend/frontend/infrastructure

**Outcome:** Reorganized project into monorepo with backend and infrastructure support:

1. **Backend Framework Decision:**
   - Recommended **Fastify + AWS Lambda** (Node.js/TypeScript)
   - Reasons: Same language as frontend, fastest Node.js framework, native TypeScript, excellent serverless support
   - Cost-effective: Pay-per-request Lambda + DynamoDB

2. **Monorepo Structure Created:**
   ```
   packages/
   ├── client/           # React frontend (moved from client-ui)
   ├── server/           # Fastify API scaffold
   ├── shared/           # Shared TypeScript types
   └── infrastructure/   # AWS CDK stacks
   ```

3. **Backend Package (packages/server):**
   - Fastify app with AWS Lambda adapter
   - Document CRUD API routes
   - Health check endpoint
   - In-memory store (DynamoDB ready)

4. **Shared Package (packages/shared):**
   - Document types
   - Drawing types
   - API request/response types
   - Common utilities (Point, Rect, etc.)

5. **Infrastructure Package (packages/infrastructure):**
   - AWS CDK with TypeScript
   - DatabaseStack (DynamoDB)
   - ApiStack (Lambda + API Gateway)
   - FrontendStack (S3 + CloudFront)

6. **Root Configuration:**
   - npm workspaces setup
   - Unified build/dev/deploy commands
   - Updated .gitignore

7. **Documentation Updated:**
   - CLAUDE.md rewritten for new structure
   - Added backend-api.md, infrastructure.md, monorepo-structure.md
   - Updated requirements.md with implementation status
